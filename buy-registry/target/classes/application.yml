server:
  port: 8000

spring:
  application:
    name: buy-registry

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8000/eureka
    register-with-eureka: false #��ע�᱾����
    fetch-registry: false
  server:
    enable-self-preservation: false #�ر����ұ���ģʽ
    eviction-interval-timer-in-ms: 10000
