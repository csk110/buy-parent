server:
  port: 7000
spring:
  application:
    name: buy-gateway
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8000/eureka
    registry-fetch-interval-seconds: 5 #��ȡ����ʱ����
zuul:
  prefix: /api
